using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using {{ module_namespace }}.Domain;
using {{ module_namespace }}.Infrastructure;
using Modulus.Core.Abstractions;

namespace {{ module_namespace }};

/// <summary>
/// Module definition for {{ module_name }}.  Add [DependsOn] for any
/// framework modules (e.g. <c>[DependsOn(typeof(DataModule))]</c>).
/// </summary>
public sealed class {{ module_name }}Module : ModulusModule
{
    public override void ConfigureServices(
        IServiceCollection services,
        IConfiguration configuration)
    {
        services.AddScoped<IProductRepository, ProductRepository>();
    }
}
