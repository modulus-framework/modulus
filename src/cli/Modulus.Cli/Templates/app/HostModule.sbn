{{ if !no_example }}using {{ root_namespace }}.Modules.Products;
{{ end }}using Modulus.Core.Abstractions;

namespace {{ root_namespace }}.Host.Modules;

/// <summary>
/// Root startup module for {{ app_name }}.  Lists every business module
/// via [DependsOn] so that Modulus auto-discovers the full graph.
/// </summary>
{{ if !no_example }}[DependsOn(typeof(ProductsModule))]
{{ end }}public sealed class {{ app_name }}HostModule : ModulusModule
{
}
