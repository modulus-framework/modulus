@page
@model Modulus.UI.Identity.Pages.Account.LoginModel
@{
    ViewData["Title"] = await Model.TextAsync("Login.Title");
}
<div class="container-tight px-0 my-4">
    <div class="card">
        <div class="card-body">
            <h2 class="card-title text-center mb-4">@await Model.TextAsync("Login.Title")</h2>
            <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                <div class="mb-3">
                    <label asp-for="Input.Email" class="form-label">@await Model.TextAsync("Login.Email")</label>
                    <input asp-for="Input.Email" class="form-control" autocomplete="username" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <div class="mb-3" x-data="mPasswordToggle">
                    <label asp-for="Input.Password" class="form-label">@await Model.TextAsync("Login.Password")</label>
                    <div class="input-group">
                        <input asp-for="Input.Password" class="form-control" autocomplete="current-password" x-bind:type="inputType" />
                        <button type="button" class="btn" x-on:click="toggle" x-text="label">Show</button>
                    </div>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label class="form-check">
                        <input asp-for="Input.RememberMe" class="form-check-input" />
                        <span class="form-check-label">@await Model.TextAsync("Login.RememberMe")</span>
                    </label>
                </div>
                <div class="form-footer">
                    <button type="submit" class="btn btn-primary w-100">@await Model.TextAsync("Login.Submit")</button>
                </div>
            </form>
        </div>
    </div>
</div>
