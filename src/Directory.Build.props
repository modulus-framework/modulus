<Project>

  <!-- ════════════════════════════════════════════════════════════════
       src/Directory.Build.props

       Chains to the root Directory.Build.props (which imports
       build/Modulus.Common.props and build/Modulus.Packaging.props),
       then marks every src/ project as a packable framework library.
       Test projects override <IsPackable> via tests/Directory.Build.props.
       ════════════════════════════════════════════════════════════════ -->

  <Import Project="$(MSBuildThisFileDirectory)..\Directory.Build.props" />

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <IsPackFrameworkLibrary>true</IsPackFrameworkLibrary>
  </PropertyGroup>

</Project>
