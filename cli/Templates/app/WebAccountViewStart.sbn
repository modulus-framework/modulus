@{
    // The Account pages (login/logout) render in the theme's account layout —
    // the same one the framework's own sign-in UI uses.
    Layout = Context.GetThemeLayout(StandardLayouts.Account);
}
